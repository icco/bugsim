# Sum positive integers

Implement `SumPositive(xs []int) int` in `solution.go` so it returns the sum of the strictly positive integers in `xs`.

- Negative numbers and zero are ignored.
- An empty or nil slice returns `0`.

Run the hidden tests from the problem screen.

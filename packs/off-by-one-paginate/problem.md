# Pagination off-by-one

A teammate added a `Paginate` helper for a list endpoint. QA flags an intermittent panic on the last page when the dataset isn't an exact multiple of the page size.

Read the snippet under `bug/` and pick the answer that best identifies the actual defect.
